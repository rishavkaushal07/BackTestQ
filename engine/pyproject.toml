[build-system]
requires = ["maturin>=1.6"]
build-backend = "maturin"

[project]
name = "trading_engine"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "trading_engine"
bindings = "pyo3"
